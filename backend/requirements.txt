# Fabric Universal Connector — Backend Dependencies
# Python 3.11+

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Data validation
pydantic==2.8.2
pydantic-settings==2.4.0

# HTTP client (async only — never use requests)
httpx==0.27.2

# Microsoft Authentication Library (Entra ID multi-tenant)
msal==1.30.0

# Azure SDK
azure-keyvault-secrets==4.8.0
azure-identity==1.18.0

# Parquet / Arrow (Open Mirroring writer)
pyarrow==17.0.0

# Scheduler (webhook renewal + poll fallback jobs)
apscheduler==3.10.4

# Salesforce Streaming API (CometD)
aiohttp==3.10.5          # CometD client requires aiohttp

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
respx==0.21.1            # Mock httpx calls in tests
pytest-cov==5.0.0
pandas==2.2.3

# JWT verification
PyJWT==2.8.0
requests==2.31.0
